# import yaml


# def get_conf_value(key_name):
#     """Function to return value from yaml file 
#     with given key name

#     Arguments:
#         key_name {str} -- Name of key

#     Returns:
#         type which is defined in yaml file -- Value from yaml file
#     """
#     with open("configuration.yaml", 'r') as stream:
#         data_loaded = yaml.safe_load(stream)
#         return data_loaded.get(key_name)
